The EntityGenerator jar make all entity classes from your DataFramework project and will save in a packege called "com.android.dataframework" in your source folder, if you are using eclipse or other IDE maybe you will need to refresh your project to see the changes. 

To run this jar you only need to pass the path of your project has a parameter.

For example:

java -jar EntityGenerator.jar /Document/MyDataFrameworkProject